{
  "version": 2,
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "installCommand": "npm install --include=dev && cd api && npm install",
  "rewrites": [
    {
      "source": "/api/((?!process-upload-queue).*)",
      "destination": "/api/src/server.js"
    }
  ],
  "functions": {
    "api/src/server.js": {
      "maxDuration": 30,
      "memory": 1024
    },
    "api/process-upload-queue.js": {
      "maxDuration": 300,
      "memory": 2048
    }
  },
  "env": {
    "NODE_ENV": "production"
  }
}